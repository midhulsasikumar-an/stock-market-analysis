import { AreaChart, BarChart, LineChart, PieChart } from 'recharts';

// Export references so this file can be type-checked/linted without unused-import warnings.
export const rechartsImportTest = {
    AreaChart,
    BarChart,
    LineChart,
    PieChart
};
